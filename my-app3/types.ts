// types.ts

// Définir les types des paramètres pour chaque écran
// types.ts (or you can place this inside ProfileScreen.tsx if you prefer)
export type ProfileScreenParams = {
    token: string;
  };
  